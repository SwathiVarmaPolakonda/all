function Rectangle(a, b) {
    var length = a;
    var width = b;

    var perimeter = 2 * (length + width);
    console.log(perimeter);
    var area = a * b;
    console.log(area);



}
Rectangle(4,5);
